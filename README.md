# SonaraAPI - API de Síntesis de Voz con Clonación

API para generación de audio con clonación de voz usando Qwen3-TTS.

## Requisitos del Sistema

### Rubberband (OBLIGATORIO)

```bash
sudo apt-get update
sudo apt-get install rubberband-cli